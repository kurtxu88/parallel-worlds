# Postgres Schema

`init.sql` creates the relational tables used by the open-source runtime:

- `guest_users`
- `user_settings`
- `stories`
- `story_events`

These tables replace the old hosted backend dependency for story state and history.
