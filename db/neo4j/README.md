# Neo4j Notes

Neo4j stores generated world graph data used during play.

The worker imports generated story structures into Neo4j, and the API gameplay engine reads that graph to stream interactive sessions.
