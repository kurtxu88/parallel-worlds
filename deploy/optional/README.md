# Optional Deployment Notes

This directory is reserved for non-default deployment templates.

The public v1 release is documented around local Docker development first. If hosted deployment examples are added later, they should be:

- provider-neutral when possible
- template-only
- free of real project identifiers, credentials, and production URLs
