# Web App

The web app is a Vue 3 + Vite frontend for Parallel Worlds.

## Commands

```bash
npm install
npm run dev
npm run build
npm run test
```

## Runtime Contract

The frontend expects:

- `VITE_API_BASE_URL`

The open-source web app does not connect directly to Supabase, Postgres, or Neo4j.
