# Web Development Setup

## Local API

Default local API base URL:

- `http://localhost:8000`

## Start the frontend

```bash
cd apps/web
npm install
npm run dev
```

## Notes

- Guest mode is enabled by default
- Story generation status is polled through the API
- Do not add hard-coded production URLs or hosted auth defaults
